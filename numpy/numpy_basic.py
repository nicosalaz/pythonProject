# forma clasica para importar la librería como módulo con el alias np
import numpy as np

# la clase array es el 'ciudadano de primera clase' de la librería
x = np.array([1, 2, 3, 4])

# notese la diferencia del objeto array con el objeto list
print (x)
print ([1,2,3,4])